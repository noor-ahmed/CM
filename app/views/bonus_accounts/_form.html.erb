<%= form_for(bonus_account) do |f| %>
  <% if bonus_account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bonus_account.errors.count, "error") %> prohibited this bonus_account from being saved:</h2>

      <ul>
      <% bonus_account.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :da_val_available %>
    <%= f.text_field :da_val_available %>
  </div>

  <div class="field">
    <%= f.label :da_val_change %>
    <%= f.text_field :da_val_change %>
  </div>

  <div class="field">
    <%= f.label :da_val_before %>
    <%= f.text_field :da_val_before %>
  </div>

  <div class="field">
    <%= f.label :itemfeature_id %>
    <%= f.text_field :itemfeature_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
